let demand = 1000
let allowance = 7000
let work = "remotely"

demand === 1000 && allowance === 7000
work === "remotely" || "onsite"

if (demand) {
    console.log("My demand met")
}
console.log(`And all be working ${work}`)